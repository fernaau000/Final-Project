{
  "name": "st-final-lstm",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "start:dist": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p dist && esbuild app.js --bundle --minify --sourcemap --outfile=dist/bundle.js && sed 's/app.js/bundle.js/' index.html > dist/index.html && cp styles.css dist/styles.css"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "engines": {
    "node": ">=18"
  }
}
